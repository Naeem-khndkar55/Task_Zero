1.clone the reop
2.fronten:
--go to frontend folder and run "npm install"
--change the  backend api link from api.js
--run "npm run dev"
3.backend:
--go to backeend : "cd backend"
--run "npm insall"
--add a .env file in backend folder and create this environment variable::-
MONGO_URI=""
JWT_SECRET=""
PORT=
NODE_ENV=development 
CLIENT_URL= "" #your frontend url


##### description

it is a task management system. where i use , React +tailwind css + react tostify+react router dom for front end.
and express, jwt, bcrypt,cors,mongoose ,mongodb for database.
it has fully fuctional login registration and CRUD operation of Tasks. try to mantain the figma design.
